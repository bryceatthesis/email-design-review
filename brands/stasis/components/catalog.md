# Stasis Component Catalog

Index of `brands/stasis/components/` — the extracted, Bryce-approved Stasis section library (extracted 2026-06-12). Every component is verbatim markup from a Bryce-approved build (`designs/welcome-quiz/cc-from-spec/01..05`) or a coupon-normalized send copy (`designs/welcome-quiz/cc-send/01..05`), with only approved values swapped for `{{SLOT}}` markers. Each component file carries its own full header (slots with approved values, media classes verbatim, dependencies) — **the file header is the authority; this catalog is the index.**

Slot markers are `{{UPPERCASE_WITH_UNDERSCORES}}` (no inner spaces). Anything in a fragment shaped like `{{ organization.name }}` or `{% coupon_code %}` is Klaviyo Liquid, not a slot — keep it byte-exact.

## Components

Ordered roughly top-of-email → bottom. "Paste level" in notes: **row** = direct `<tr>` child of the host 600px `.wrap` table; **fragment** = pastes inside a host `<td>`; **panel row** = `<tr>` inside another component's panel table.

| Component | Purpose | Slots | Source (file + lines) | Required 480px media classes | Notes |
|---|---|---|---|---|---|
| `promo-strip-yellow.html` | Full-width yellow announcement strip (module 1) — one centered Antarctica-500 line on brand yellow #EEEF78. | `STRIP_COPY` | cc-from-spec/02, L110–113 | `.px` `.m-banner-td` `.m-banner` | Row. Yellow pinned (this IS the yellow variant); no VML needed. If copy references a discount code, the coupon zone is governed by send-prep-spec §2 — pair with `coupon-code-row-yellow` in send copies. |
| `coupon-code-row-yellow.html` | Yellow coupon-code row that sits DIRECTLY under the yellow promo strip; strip + row = one §2 coupon zone (renders in both modes). | none (Liquid verbatim) | cc-send/02, L120–129 | `.wsc-code-y` | Row. SEND-COPY-ONLY (carries `{% coupon_code 'Welcome_Discount' %}`); review builds omit it. Never without the context strip directly above (§2 forbids bare code lines). Intentional oddities — do not "fix": `Antartica` misspelling (Lucida fallthrough), bg #EEEF7A vs strip #EEEF78. |
| `hero-art-overlay.html` | Full-bleed hero — live text (wordmark, H1, subhead, pill CTA) over recomposed text-free background art; VML rect fallback for Outlook; artwork's bottom bleed as its own `<img>` row. | `HERO_BG_COLOR` `HERO_BG_URL` `HERO_BG_BOTTOM_URL` `WORDMARK_URL` `HEADLINE` `SUBHEAD` `CTA_TEXT` `CTA_HREF` | cc-from-spec/02, L115–158 | `.px` `.h-hero` `.cta-link` `.dbr` `.m-hero` `.m-logo` `.m-h1row` `.m-subrow` `.m-ctarow` `.m-sub` `.m-cta` | Row (×2 tr: text zone + bleed img). BG art must be TEXT-FREE; bg td + bleed img must come from the same recomposition. Wordmark is an image, never typed (§4); its link is pinned, not a slot. Keep both halves of every mso/!mso conditional pair. Display weights 500, not 900. |
| `wordmark-row.html` | Centered black Stasis wordmark row — the standard logo module between bands. | `WORDMARK_URL` `BG` `PADDING` | cc-from-spec/03, L94–96 (cream instance: cc-from-spec/05, L97–99) | `.wm` (+ `.wm-td` rule only if using the E5 cream class) | Row. One declared deviation from verbatim: `{{BG}}` added to host the cream variant. Approved pairs: #FFFFFF + `35px 40px 47px 40px` (E3) / #F4EFEB + `37px 40px 43px 40px` (E5). §4 logo-is-the-logo: always the approved rendition (logo-usage.md, shas pinned in header). |
| `cta-pill.html` | Brand pill CTA — VML roundrect for Outlook paired with border-radius table + anchor; orange #FF541E fill, black uppercase tracked label. | `CTA_TEXT` `CTA_HREF` (each ×2: VML + HTML) | cc-from-spec/02, L214–220 (pattern repeats L139–145, 187–193, 330–336; size variant L355–361) | `.cta-link` (+ `.m-cta` only with the hero-variant anchor class) | **Fragment** — button only, paste inside a host td. Ships standard geometry (21px 56px / VML 57×236); header documents closing-lockup (17px 50px / 50×222) and hero (`.m-cta`) variants. VML box is fixed-pixel: re-measure width if the label differs materially from TRY STASIS. |
| `condition-pills.html` | Left-aligned vertical stack of single-color uppercase condition pills (one single-cell table per pill). | `PILL_1_COPY` `PILL_2_COPY` `PILL_3_COPY` `PILL_BG` (×2 per pill) | cc-from-spec/03, L124 (pill tables only; module-6 host row/lead-in NOT included) | `.pill` | **Fragment** — paste inside a host td (E3: `padding:24px 40px 0 40px` with lead-in `<p>` above). Count changes: duplicate a non-last unit; every pill except the last carries `margin:0 0 14px 0`, last is `margin:0`. Approved bg #c3d4ff periwinkle; ink #0E1A38, radius 999px, nowrap pinned. |
| `two-col-list.html` | Lime panel, 44%/56% two-column: big heading left, hairline-ruled list right; columns stack on mobile. | `HEADING_HTML` `ITEM_1` `ITEM_2` `ITEM_3` `ITEM_LAST` | cc-from-spec/03, L132–140 + closing `</td></tr>` from L152 | `.col-stack` `.col-left` `.h-lg` | Row. The module's trailing §5.1 CTA (E3 L141–151) is intentionally NOT included — drop `cta-pill` after the two-column table if needed. Rule pattern is divider-between-rows: `ITEM_LAST` has no border-bottom; keep that when changing count. Lime #EEEF78 / ink #0E1A38 baked. |
| `checklist-rows.html` | Module-9 benefit checklist — hairline-ruled rows, copy left + text `&#10003;` check glyph right; final row closes the list (bottom rule + deep padding). | `ROW_1_COPY` `ROW_2_COPY` `ROW_3_COPY` | cc-from-spec/02, L239–245 | `.px` | Row (white module). Approved build is exactly 3 rows; to extend, duplicate a middle row ABOVE the final row — never after it. Rules are 1px solid #000000 borders, no images; check is text, not an image. |
| `outlined-infographic-box.html` | Bordered (1.5px #0E1A38, 16px radius) infographic box on cream: uppercase headline cell, divider, then 30-dot progress grid (pure CSS shapes) beside side copy. | `HEADLINE` `SIDE_COPY` | cc-from-spec/05, L102–162 | `.h1` `.box-td` `.box-head` `.box-grid` `.box-copy` `.dot-cell` `.dot` `.dot-ring` `.dot-ring-in` | Row (cream host bg baked). GRID INTEGRITY: 30 `td.dot-cell`, 29 `div.dot`, 1 `div.dot-ring` — the dot count IS the "30 days" claim; re-verify after ANY edit. `&dagger;` in side copy needs a matching disclaimer module in the host. `.h1` mobile is 27px (E5), not E3's 34px. `border-collapse:separate` required. |
| `stats-panel-dark.html` | Black §6.2 stats panel — 2-col icon grid, 5 stat cells across 3 bands (flat icon crop, 1px white rule, white 75px numeral, 21px/500 label), hand-capsule photo bleeding flush right across rows 2–3. | per cell n=1..5: `STATn_ICON_URL` `STATn_ICON_W` `STATn_ICON_H` `STATn_NUM` `STATn_LABEL`; plus `BLEED_PHOTO_URL` | cc-from-spec/02, L247–328 | `.stat-num` `.sgut` `.sgap` `.scell` `.photo-clip` `.hand-img` `.slabel` `.dbr` | Row. Geometry per e2-grid-geometry.md (gutters 65/57/74, cells 202px) — do NOT retune. Labels keep designed `<br class="dbr"/>` breaks and may overflow the cell, as designed; trailing `&dagger;` is fixed markup outside the slot and needs the host survey disclaimer. Closing CTA NOT included — drop `cta-pill` into a black host td. No VML. |
| `stats-panel-light.html` | Light #F7F7F7 §6.2 two-cell stat panel — same cell pattern as dark, with black rules/numerals/labels. | `CELL1_ICON_URL` `CELL2_ICON_URL` `CELL1_STAT` `CELL2_STAT` `CELL1_LABEL_HTML` `CELL2_LABEL_HTML` | cc-from-spec/05, L193–224 | `.stat-num` `.sgut` `.sgap` `.scell` `.slabel` `.dbr` | Row. Icon display sizes + icon-row paddings are a matched measured pair and deliberately NOT slots — a different icon requires re-measuring both. Do not mix this panel's icons with the module-9 #F4F4F4-baked variant crops. Labels carry `dbr` breaks + `&dagger;` (host disclaimer required). Trailing 36px spacer row is part of the module. |
| `pricing-cards-2col.html` | Two white pricing cards (14px radius) side by side, 50/50 — SAVE eyebrow, big dollar amount, term copy, SAVE NOW orange CTA pill per card; stacks on mobile. | `CARD1_SAVE_AMOUNT` `CARD2_SAVE_AMOUNT` `CARD1_TERM_COPY` `CARD2_TERM_COPY` `CARD1_CTA_HREF` `CARD2_CTA_HREF` (each href ×2: VML + anchor) | cc-from-spec/05, L273–312 | `.col-stack` `.col-gap-l` `.col-gap-r` `.btn` | **Panel row** — pastes inside a §6.1 lime panel table (E5 host: module 10's panel, L266–267 open / L313–314 close), never directly into the 600px wrap. SAVE eyebrow + SAVE NOW label are baked copy, not slots; fill each card's href in BOTH the v:roundrect and the `<a class="btn">`. Card bg #FFFFFF on lime #EEEF78; `border-collapse:separate` required; keep all four conditional-comment markers per card. |
| `testimonial-card-centered.html` | §6.1 centered quote card (uppercase Antarctica heading + upright Tiempos quote + Antarctica attribution) — the E4 trio, parameterized per approved colorway. | `ROW_PADDING` `CARD_BG` (×2) `TOP_CORNERS` `CARD_PADDING` `HEADING_SIZE` `HEADING` `HEADING_COLOR` `QUOTE_PARAGRAPHS` `QUOTE_COLOR` `ATTRIBUTION` `ATTRIBUTION_COLOR` | cc-from-spec/04 — lime L96–104, peach L128–136, teal L162–170 | `.qh` `.qq` `.qpad` | Row. Fill every slot from ONE colorway row of the header's value-set table (lime / peach / teal) — never mix rows. No approved centered cream: cream testimonials are only the photo-flavor Devon card. Serif quote is UPRIGHT, never italic (§3/§6.1); `border-collapse:separate` required for the radius. Square top corners when abutting a photo/seam row, 18px standalone. |
| `testimonial-card-photo.html` | §6.1 photo-top quote card (photo inside the card, left-aligned multi-paragraph Tiempos quote + em-dash attribution in the SAME cell) — the E2 Melinda pattern. | `CARD_BG` `PHOTO_PROVENANCE` `PHOTO_SRC` `PHOTO_ALT` `QUOTE_PARAGRAPHS` `ATTRIBUTION` | cc-from-spec/02, L197–210 | `.px` `.kl-img-100` | Row. One approved colorway: lime #EEEF78. Photo is a §8.5 derived crop sourced via `email-image-source` (gaps stop the build); provenance comment is mandatory. Attribution is Antarctica 400/20px — measured, do not normalize against the other flavors. Structurally distinct from the cream card — never borrow values across the two photo flavors. |
| `testimonial-card-photo-cream.html` | §6.1 cream photo-top quote card (photo, single quote, attribution each in its OWN row) — the E3 Devon pattern; the only approved cream testimonial. | `CARD_BG` `PHOTO_PROVENANCE` `PHOTO_SRC` `PHOTO_ALT` `QUOTE_PARAGRAPHS` `ATTRIBUTION` | cc-from-spec/03, L160–173 | none (mobile via host `.wrap` fluid override) | Row. One approved colorway: cream #F4EFEB, ink #0E1A38. 17px/1.5 quote, Antarctica 600/14px attribution — measured, do not normalize. Extracted as its own file rather than force-merged: fidelity beats forced unification. Photo per §8.5 via `email-image-source`. |
| `promo-band-black.html` | §6.3 black promo band: lime $50 pill + white context sub-line + desk/mob coupon-code pair — a complete §2 coupon zone. | `PILL_TEXT` `CONTEXT_LINE` | cc-send/05, L186–197 | `.pill50` `.wsc-desk` `div.wsc-mob` | Row. Coupon zone is BINDING per send-prep-spec §2 (supersedes wsc-send-spec's match-the-original call). The code pair carries Klaviyo Liquid and is SEND-COPY-ONLY — review builds ship pill + context line only. Keep the trailing "with code:" context phrasing; never let it render with nothing under it in a send. Code pair is deliberately Helvetica, not Antarctica. |
| `closing-lockup.html` | Closing lockup (§5.2) — cream dome rising from black (§8), STASIS + STIMULANT pill pair, BALANCE FOR BOLD MINDS in cobalt, SHOP NOW pill on cream. | `CTA_HREF` (×2: VML + HTML) | cc-from-spec/02, L343–362 | `.px` `.h-closer` `.cta-link` | Row (×2 consecutive tr: dome + lockup). The module ABOVE must be black #000000 — the dome is a black td with the cream dome div rising out of it. Dome is CSS-only; Outlook gets a flat cream band, as approved. All copy is the pinned brand lockup — intentionally not slotted. SHOP NOW uses the size-variant pill (50×222). |
| `legal-footer.html` | White legal footer for every send copy: FDA disclaimer + unsubscribe/preferences/privacy line + address/year line, all Klaviyo Liquid verbatim (send-prep-spec §1). | none (Liquid verbatim) | cc-send/02, L382–387 (identical in cc-send/03/04/05) | `.px` | Row. SEND-COPY-ONLY — review builds carry no legal footer. `{{ organization.* }}` / `{% … %}` are Liquid, not slots; staged drafts must keep them byte-exact (klaviyo-stage verification). E1 flavor differs (omits `{% manage_preferences %}`, 20px `.px`) — keep per-email; this file is the E2–E5 default. White bg pinned. |

## Usage rules

1. **Paste, fill, don't redraw.** Components are extracted verbatim from Bryce-approved builds. Use one by pasting the fragment at its documented paste level, filling only the `{{SLOT}}` markers, and copying the listed 480px media rules verbatim into the host's `@media only screen and (max-width:480px){ … }` block. The host document must carry the light-only pin: both metas (`<meta name="color-scheme" content="light only">` + `<meta name="supported-color-schemes" content="light only">`) and the `:root, body { color-scheme: light only; }` rule.
2. **Non-slot markup is frozen.** Editing anything outside a slot — paddings, colors, weights, VML geometry, conditional-comment structure, pinned copy — is a design change and requires Bryce review before it ships. The pinned values exist because they were measured against approved renders.
3. **No component fits? Propose, don't improvise.** If a layout maps to no catalog entry, propose a new component to Bryce; extract it into this library only after an approved build contains it. Never improvise a one-off section inline.
4. **Colorways are closed sets.** The colorway / value-set tables in component headers (testimonial colorways, wordmark BG+padding pairs, cta-pill geometry variants, pill backgrounds) are the approved palette. Fill every slot from one row; a new color, size, or variant value is a Bryce call, not a fill-in.
5. **Net-new mapping starts here.** Section mapping for net-new emails (email-design Step 2) happens against this catalog: map each section of Bryce's copy to a component row above, and flag anything unmapped as a rule-3 proposal or an imagery-sourcing dependency before directions are drafted.
